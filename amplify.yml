version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd rift-rewind-proj && npm ci
    build:
      commands:
        - npm run build
  artifacts:
    files:
      - '**/*'
  cache:
    paths:
      - rift-rewind-proj/node_modules/**/*